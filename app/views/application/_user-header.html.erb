<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to root_path, class: 'navbar-brand' do %>
  <%= image_tag 'elearning.png', width: '30', height: '30', class: 'd-inline-block align-top' %>
  <div class="navbar-brand">
    eLearning
  </div>
<% end %>
<div class="collapse navbar-collapse" id="navbarNavDropdown">
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link" href="#">Home
        <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Courses
      </a>
      <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <a class="dropdown-item" href="#">Action</a>
        <a class="dropdown-item" href="#">Another action</a>
        <a class="dropdown-item" href="#">Something else here</a>
      </div>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Organization</a>
    </li>
  </ul>
</div>
<form class="form-inline md-form form-sm active-purple-2 mt-2">
  <input class="form-control form-control-sm mr-3 w-75" type="text" placeholder="Search" aria-label="Search">
  <i class="fas fa-search" aria-hidden="true"></i>
</form>
<ul class="navbar-nav">
  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <%= User.find(session[:ownable_id]).profile.nickname + '(User)' if session[:ownable_type] == 'User' %>
      <%= Organization.find(session[:ownable_id]).name + '(Organization)' if session[:ownable_type] == 'Organization' %>
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
      <%= link_to current_user.profile.nickname + '(User)', change_operator_url(ownable_id: current_user.id, ownable_type: 'User'), 
          method: :post, class: 'dropdown-item' %>
      <% current_user.organizations.map do |org| %>
        <%= link_to org.name + '(Organization)', change_operator_url(ownable_id: org.id, ownable_type: 'Organization'), 
          method: :post, class: 'dropdown-item' %>
      <% end %>
    </div>
  </li>
</ul>
<%= link_to('Logout', destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger') %>
</nav>
