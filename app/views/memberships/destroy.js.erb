let leave_button = $('#leave-organization-button')
leave_button.after($('<%= j render partial: 'organizations/join_button', locals: { organization: @organization } %>'));
leave_button.remove();
force_flash('<%= j render "layouts/flash" %>');
