<div class="card certificate-card" style="width: 18rem;">
  <img class="card-img-top" src="" alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title"><%= certificate.course.name %></h5>
    <p class="card-text">Earned: <%= certificate.completion_record.date %></p>
    <a href="<%= certificate_path(certificate) %>" class="btn btn-primary">See certificate</a>
  </div>
</div>