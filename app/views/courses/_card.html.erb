<hr>
<strong>Name:</strong><%= course.name %><br/>
<strong>Duration:</strong><%= course.duration %><br/>
<strong>Difficulty:</strong><%= course.difficulty %><br/>
<%= link_to 'More', course_path(course), class: 'btn btn-md btn-primary' %>